<h1>Listing asset_graphs</h1>

<table>
  <tr>
    <th>Graph</th>
    <th>File</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @asset_graphs.each do |asset_graph| %>
  <tr>
    <td><%= asset_graph.graph %></td>
    <td><%= asset_graph.file %></td>
    <td><%= link_to 'Show', asset_graph %></td>
    <td><%= link_to 'Edit', edit_asset_graph_path(asset_graph) %></td>
    <td><%= link_to 'Destroy', asset_graph, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Asset graph', new_asset_graph_path %>
