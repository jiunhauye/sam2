<%= form_for(@asset_graph) do |f| %>
  <% if @asset_graph.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@asset_graph.errors.count, "error") %> prohibited this asset_graph from being saved:</h2>

      <ul>
      <% @asset_graph.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :graph %><br />
    <%= f.text_area :graph %>
  </div>
  <div class="field">
    <%= f.label :file %><br />
    <%= f.text_field :file %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
